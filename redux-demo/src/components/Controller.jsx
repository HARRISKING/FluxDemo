import React from 'react';
import { createStore } from 'redux';

// const store = createStore();

export default class Controller extends React.Component {

    render(){
        return (
            <div style={{width:200, margin:'0 auto', border:'1px solid', textAlign:'center'}}>
                Redux学习相关
            </div>
        )
    }
}